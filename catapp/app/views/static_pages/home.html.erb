<h1>StaticPages#home</h1>
<%= form_with(url: home_path, method: :get, local: true)do |f| %>

  <p>
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </p>

  <%= f.submit 'Search' %>

<% end %>

<%= render 'show', photos: @photo %>
